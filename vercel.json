{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "functions": {
        "app/api/*.py": {
            "runtime": "python"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/app/api/"
        }
    ]
}